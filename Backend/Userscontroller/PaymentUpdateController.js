const userModel = require("../Models/usermodel");
const guideModel = require("../Models/GuideModel");
const TravelerModel = require("../Models/TravelerModel");
const ParkingOwnerModel = require("../Models/ParkingOwner");
const ParkingAreaModel = require("../Models/Parking_Area_Model");

const Payment_Parking = async function(req,res)
{
    try{
        //send the details of the payment if success store it else reject check in frontend as well
    }
    catch(error)
    {
        console.log(error)
    }
}